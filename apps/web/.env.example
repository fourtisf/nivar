# --- NIVAR web ---
# Base URL of the Fastify api. Only needed once the engine is wired to the
# server (milestones 2+). Milestone 1 runs fully in-memory with no api.
NEXT_PUBLIC_API_URL=http://localhost:4000
